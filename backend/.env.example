PORT=3001
HOST=0.0.0.0
NODE_ENV=production
JWT_SECRET=sua_chave_secreta_jwt_longa_e_segura_aqui

# Conexão com o PostgreSQL (usando container Docker interno ou externo)
DATABASE_URL=postgresql://pontoflow:pontoflow123@postgres:5432/pontoflow_db

# Ou variáveis separadas:
DB_HOST=postgres
DB_PORT=5432
DB_USER=pontoflow
DB_PASSWORD=pontoflow123
DB_NAME=pontoflow_db
